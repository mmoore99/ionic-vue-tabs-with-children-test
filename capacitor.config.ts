import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.vued.tabs.with.children.test',
  appName: 'ionic-vued-tabs-with-children-test',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
